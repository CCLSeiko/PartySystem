export function generateStaticParams() {
  return [{ id: 'placeholder' }];
}

export default function SubscriptionEditLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
